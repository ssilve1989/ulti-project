---
import { DevMockDataToggle } from '../components/DevMockDataToggle';

export interface Props {
  title: string;
  description?: string;
}

const { title, description = "A FFXIV opportunity for NA players to progress thru ENDGAME CONTENT and achieve their 1st-time clears!" } = Astro.props;
const isDevelopment = import.meta.env.DEV;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <!-- Development Mock Data Toggle -->
    {isDevelopment && <DevMockDataToggle client:load />}

    <!-- Theme Toggle Button -->
    <button id="theme-toggle" class="theme-toggle" aria-label="Toggle theme">
      <span class="theme-toggle-icon">🌙</span>
    </button>

    <header class="header">
      <nav class="nav-container">
        <div class="nav-content">
          <div class="nav-brand">
            <a href="/" class="brand-link">
              Ulti Project
            </a>
          </div>
          <div class="nav-links">
            <a href="/" class="nav-link">
              Home
            </a>
            <a href="/signups" class="nav-link">
              Signups
            </a>
            <a href="https://discord.gg/sausfest" target="_blank" rel="noopener noreferrer" 
               class="nav-cta">
              Join Discord
            </a>
          </div>
        </div>
      </nav>
    </header>

    <main class="main-content">
      <slot />
    </main>

    <footer class="footer">
      <div class="footer-container">
        <div class="footer-content">
          <div class="footer-info">
            <p>&copy; 2024 Ulti Project - Sausfest Community</p>
            <p>Aether Data Center • FFXIV Endgame Content</p>
          </div>
          <div class="footer-links">
            <a href="https://discord.gg/sausfest" target="_blank" rel="noopener noreferrer" 
               class="footer-link">
              Discord
            </a>
            <a href="#" class="footer-link">Twitter</a>
            <a href="#" class="footer-link">Twitch</a>
            <a href="#" class="footer-link">YouTube</a>
          </div>
        </div>
      </div>
    </footer>

    <script>
      // Theme management
      const themeToggle = document.getElementById('theme-toggle');
      const themeIcon = themeToggle?.querySelector('.theme-toggle-icon');
      
      // Get saved theme or default to dark
      const savedTheme = localStorage.getItem('theme') || 'dark';
      document.documentElement.setAttribute('data-theme', savedTheme);
      
      // Update icon based on current theme
      function updateThemeIcon(theme: string) {
        if (themeIcon) {
          themeIcon.textContent = theme === 'dark' ? '☀️' : '🌙';
        }
      }
      
      // Initialize icon
      updateThemeIcon(savedTheme);
      
      // Theme toggle handler
      themeToggle?.addEventListener('click', () => {
        const currentTheme = document.documentElement.getAttribute('data-theme');
        const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
        
        document.documentElement.setAttribute('data-theme', newTheme);
        localStorage.setItem('theme', newTheme);
        updateThemeIcon(newTheme);
      });
    </script>
  </body>
</html>

<style>
  /* Notification animations */
  @keyframes slideIn {
    from {
      transform: translateX(100%);
      opacity: 0;
    }
    to {
      transform: translateX(0);
      opacity: 1;
    }
  }
  
  @keyframes slideOut {
    from {
      transform: translateX(0);
      opacity: 1;
    }
    to {
      transform: translateX(100%);
      opacity: 0;
    }
  }

  /* Development Mock Data Toggle */
  :global(.dev-toggle-container) {
    position: fixed;
    top: 5rem;
    right: 1rem;
    z-index: 999;
    background-color: var(--bg-secondary);
    border: 2px solid var(--warning-border);
    border-radius: 0.5rem;
    padding: 0.75rem;
    box-shadow: 0 4px 6px var(--shadow);
  }
  
  :global(.dev-toggle-content) {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  
  :global(.dev-toggle-label) {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--warning-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  :global(.dev-toggle-switch) {
    position: relative;
    display: inline-block;
    width: 3rem;
    height: 1.5rem;
    cursor: pointer;
  }
  
  :global(.dev-toggle-input) {
    opacity: 0;
    width: 0;
    height: 0;
  }
  
  :global(.dev-toggle-slider) {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: var(--bg-tertiary);
    transition: 0.3s;
    border-radius: 1.5rem;
    border: 1px solid var(--border-primary);
  }
  
  :global(.dev-toggle-slider:before) {
    position: absolute;
    content: "";
    height: 1rem;
    width: 1rem;
    left: 0.125rem;
    bottom: 0.125rem;
    background-color: var(--text-secondary);
    transition: 0.3s;
    border-radius: 50%;
  }
  
  :global(.dev-toggle-input:checked + .dev-toggle-slider) {
    background-color: var(--bg-accent);
    border-color: var(--bg-accent);
  }
  
  :global(.dev-toggle-input:checked + .dev-toggle-slider:before) {
    transform: translateX(1.5rem);
    background-color: var(--text-inverse);
  }
  
  :global(.dev-toggle-text) {
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--text-secondary);
    min-width: 4rem;
  }

  /* CSS Custom Properties for Theme System */
  :root {
    /* Light theme colors */
    --bg-primary: #ffffff;
    --bg-secondary: #f8fafc;
    --bg-tertiary: #e2e8f0;
    --bg-accent: #1e40af;
    --bg-accent-hover: #1d4ed8;
    --text-primary: #1f2937;
    --text-secondary: #4b5563;
    --text-tertiary: #6b7280;
    --text-accent: #1e40af;
    --text-accent-hover: #1d4ed8;
    --text-inverse: #ffffff;
    --border-primary: #e5e7eb;
    --border-secondary: #d1d5db;
    --shadow: rgba(0, 0, 0, 0.1);
    --warning-bg: #fef3c7;
    --warning-text: #92400e;
    --warning-border: #f59e0b;
    --success-bg: #d1fae5;
    --success-text: #065f46;
    --purple-accent: #7c3aed;
    --purple-accent-hover: #8b5cf6;
  }
  
  [data-theme="dark"] {
    /* Dark theme colors */
    --bg-primary: #111827;
    --bg-secondary: #1f2937;
    --bg-tertiary: #374151;
    --bg-accent: #3b82f6;
    --bg-accent-hover: #2563eb;
    --text-primary: #f9fafb;
    --text-secondary: #d1d5db;
    --text-tertiary: #9ca3af;
    --text-accent: #60a5fa;
    --text-accent-hover: #3b82f6;
    --text-inverse: #ffffff;
    --border-primary: #374151;
    --border-secondary: #4b5563;
    --shadow: rgba(0, 0, 0, 0.3);
    --warning-bg: rgba(251, 191, 36, 0.1);
    --warning-text: #fbbf24;
    --warning-border: #f59e0b;
    --success-bg: rgba(16, 185, 129, 0.1);
    --success-text: #10b981;
    --purple-accent: #a78bfa;
    --purple-accent-hover: #8b5cf6;
  }

  /* Base Styles */
  html {
    font-family: system-ui, sans-serif;
  }
  
  body {
    background-color: var(--bg-primary);
    color: var(--text-primary);
    min-height: 100vh;
    margin: 0;
    transition: background-color 0.3s ease, color 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  /* Theme Toggle Button */
  .theme-toggle {
    position: fixed;
    top: 1rem;
    right: 1rem;
    z-index: 1000;
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    background-color: var(--bg-accent);
    color: var(--text-inverse);
    border: 2px solid var(--border-primary);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px var(--shadow);
  }
  
  .theme-toggle:hover {
    background-color: var(--bg-accent-hover);
    transform: scale(1.05);
  }

  /* Header Styles */
  .header {
    background-color: var(--bg-secondary);
    border-bottom: 1px solid var(--border-primary);
  }
  
  .nav-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  
  .nav-content {
    display: flex;
    justify-content: space-between;
    height: 4rem;
  }
  
  .nav-brand {
    display: flex;
    align-items: center;
  }
  
  .brand-link {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-accent);
    text-decoration: none;
    transition: color 0.3s ease;
  }
  
  .brand-link:hover {
    color: var(--text-accent-hover);
  }
  
  .nav-links {
    display: flex;
    align-items: center;
    gap: 2rem;
  }
  
  .nav-link {
    color: var(--text-secondary);
    text-decoration: none;
    padding: 0.5rem 0.75rem;
    border-radius: 0.375rem;
    font-size: 0.875rem;
    font-weight: 500;
    transition: color 0.3s ease;
  }
  
  .nav-link:hover {
    color: var(--text-primary);
  }
  
  .nav-cta {
    background-color: var(--bg-accent);
    color: var(--text-inverse);
    text-decoration: none;
    padding: 0.5rem 1rem;
    border-radius: 0.375rem;
    font-size: 0.875rem;
    font-weight: 500;
    transition: background-color 0.3s ease;
  }
  
  .nav-cta:hover {
    background-color: var(--bg-accent-hover);
  }

  /* Main Content */
  .main-content {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  /* Footer */
  .footer {
    background-color: var(--bg-secondary);
    border-top: 1px solid var(--border-primary);
    margin-top: 4rem;
  }
  
  .footer-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }
  
  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .footer-info {
    color: var(--text-tertiary);
    font-size: 0.875rem;
  }
  
  .footer-info p {
    margin: 0;
  }
  
  .footer-info p:not(:first-child) {
    margin-top: 0.25rem;
  }
  
  .footer-links {
    display: flex;
    gap: 1rem;
  }
  
  .footer-link {
    color: var(--text-tertiary);
    text-decoration: none;
    transition: color 0.3s ease;
  }
  
  .footer-link:hover {
    color: var(--text-primary);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .nav-content {
      flex-direction: column;
      height: auto;
      padding: 1rem 0;
    }
    
    .nav-links {
      margin-top: 1rem;
      gap: 1rem;
    }
    
    .footer-content {
      flex-direction: column;
      text-align: center;
      gap: 1rem;
    }
    
    .theme-toggle {
      top: 0.5rem;
      right: 0.5rem;
      width: 2.5rem;
      height: 2.5rem;
      font-size: 1rem;
    }
    
    :global(.dev-toggle-container) {
      top: 4rem;
      right: 0.5rem;
      padding: 0.5rem;
    }
    
    :global(.dev-toggle-content) {
      gap: 0.25rem;
    }
    
    :global(.dev-toggle-text) {
      font-size: 0.625rem;
      min-width: 3rem;
    }
  }
</style> 
