pre-commit:
  parallel: true 
  commands:
    lint: 
      run: pnpm check --diagnostic-level=error
    typecheck:
      run: pnpm typecheck
    test:
      run: pnpm test --run

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
